<h2>Reply to this topic:</h2>
<%= form_for([@article, @comment, @discussion, @discussion.replies.build]) do |f| %>
  <p>
    <%= f.label :Reply %><br>
    <%= f.text_area :body %>
  </p>
    <p>
    <%= f.label :User %><br>
    <%= f.text_field :userName %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>